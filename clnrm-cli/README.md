# clnrm-cli

Pure CLI crate generated by `ggen` from RDF definitions.

## Current Status

⚠️ **Generation in progress**: The template is encountering a Tera parsing error (`__tera_one_off`). This needs to be resolved in the `ggen` framework.

## Manual Workaround

Until the template rendering issue is fixed, you can manually create the crate structure based on `docs/clnrm-cli-v2.ttl`.

## Setup (When Fixed)

1. Generate the CLI:
   ```bash
   /Users/sac/ggen/target/release/ggen template generate -t ../templates/cli-v2/cli-stack.tmpl -r ../docs/clnrm-cli-v2.ttl -o . -f
   ```
2. Build: `cargo build`

## Regenerating

After modifying template or RDF:
```bash
/Users/sac/ggen/target/release/ggen template generate -t ../templates/cli-v2/cli-stack.tmpl -r ../docs/clnrm-cli-v2.ttl -o . -f
```

## Issue Tracking

The template rendering fails with: `Failed to parse '__tera_one_off'`

This appears to be an internal Tera error when trying to render the template body. The issue likely needs to be fixed in `ggen`'s template rendering code or the template syntax needs to be simplified.